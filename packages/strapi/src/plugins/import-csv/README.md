# Strapi plugin import-csv

Only required for initial use when importing legacy Drupal 7 CSV data.

Needs to be explicitly enabled in `packages/strapi/config/plugins.ts`.

@todo Move "helper" into actual services

## Usage

After building, run in `packages/strapi`:

```
> npm run cli:import-csv
```
